# Astralyn Environment Variables (Phase 0 Safe Template)
# Note: Real secrets and bindings are managed locally via Wrangler and .dev.vars in later phases.

NODE_ENV=development
VITE_API_URL=http://localhost:5173
