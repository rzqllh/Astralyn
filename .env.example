# Astralyn Environment Template (Phase 3B)
# Note: Real secrets are configured in .dev.vars (local) or Cloudflare Secrets (preview/production).
# Never put real secrets, tokens, or private keys in this file or any tracked file.

NODE_ENV=development
VITE_API_URL=http://localhost:5173

# Better Auth Configuration
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:5173

# Google OAuth Provider
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
